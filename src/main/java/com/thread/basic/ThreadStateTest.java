package com.thread.basic;

/**
 * 线程状态测试
 *
 * 线程状态分为
 *
 * NEW : 在start之前
 * RUNNABLE：start之后，分配时间片（processor）之前
 * BLOCKED ：锁阻塞，如 synchronized
 * WAITING: 等待状态，包括 wait() ， join()
 * TIMED_WAITING ：限时等待状态，包括 sleep(time)， wait(time), join(time)
 * TERMINATED ： 终结状态
 *
 *
 */
public class ThreadStateTest {




}
